# roles/link_sites/templates/access_token.yaml.j2
apiVersion: skupper.io/v2alpha1
kind: AccessToken
metadata:
  name: {{ access_token_name }}
spec:
  ca: |
    {{ ca.splitlines()|join('\n    ') }}
  code: "{{ code }}"
  url: "{{ url }}"
